{
    "scripts": {
        "render": "node render.js",
        "watch": "nodemon render.js",
        "build": "rm -rf ./dist && mkdir -p ./dist && node render.js template.hbs data.json ./dist/index.html && cp ./assets/* ./dist"
    },
    "dependencies": {
        "handlebars": "^4.7.8",
        "html-minifier-next": "^5.1.5"
    },
    "devDependencies": {
        "nodemon": "^3.1.9"
    }
}
