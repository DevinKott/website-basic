{
    "scripts": {
        "render": "node render.js",
        "watch": "nodemon render.js",
        "build": "rm -rf ./dist && mkdir -p ./dist && node render.js template.hbs data.json ./dist/index.html && cp robots.txt ./dist && cp humans.txt ./dist"
    },
    "dependencies": {
        "handlebars": "^4.7.8",
        "html-minifier": "^4.0.0"
    },
    "devDependencies": {
        "nodemon": "^3.1.9"
    }
}